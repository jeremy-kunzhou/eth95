#!/usr/bin/env bash

# copy this file to HOME directory: ~
# only to fix husky runtime node version using nvm
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# mark the target node version in ./.nvmrc
if test -f ".nvmrc"; then nvm use
fi